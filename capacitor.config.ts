import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.weather.app',
  appName: 'weather-capacitor-app',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
